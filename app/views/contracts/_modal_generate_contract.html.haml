#contract-modal.modal.fade
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"data-dismiss" => "modal", type: "button"} ×
        %h3= t('.title')
      = form_tag(new_contract_path, method: :get) do |f|
        .modal-body
          = hidden_field_tag :renter_name
          = hidden_field_tag :renter_dni
          .token
            %b= t('.search_renter')
            = text_field_tag :renter_token
          .token
            %b= t('.search_property')
            = text_field_tag :property_token

        .modal-footer
          %button{"data-dismiss" => "modal"}= t('.cancel')
          = submit_tag t('.submit'), class: 'modal-submit'
